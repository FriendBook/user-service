apiVersion: v1
kind: Service
metadata:
    name: "friendbook-1bb5"
    labels:
        app: "friendbook-1bb5"
spec:
    type: LoadBalancer
    ports:
    - port: 4000
      targetPort: 4000
      protocol: TCP
      name: http
    selector:
        app: "friendbook-1bb5"